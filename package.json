{
  "name": "oh-my-pi-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "simple-git-hooks",
    "format": "bun x prettier --write \"**/*.{js,mjs}\"",
    "format:check": "bun x prettier --check \"**/*.{js,mjs}\""
  },
  "devDependencies": {
    "lint-staged": "^15.5.2",
    "simple-git-hooks": "^2.12.1"
  },
  "lint-staged": {
    "*.{js,mjs}": "bun x prettier --write"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  }
}
